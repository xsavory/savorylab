import type { Participant } from "@/types/schema";

export function buildQRData(data: Participant): string {
  
}

export function parseQRData(qrData: string): Participant | null {
  
}

export function downloadQRImage(qrData: string) {
  
}